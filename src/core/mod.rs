// Фаза 1: GameTime, Health, Team, Events
// Файлы: time.rs, health.rs, team.rs, resources.rs, events.rs

use bevy::prelude::*;

pub struct CorePlugin;

impl Plugin for CorePlugin {
    fn build(&self, _app: &mut App) {}
}
